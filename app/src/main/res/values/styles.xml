<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MainTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:background">@color/MainBackground</item>
        <item name="android:colorPrimaryDark">@color/MainBackground</item>
        <item name="android:colorPrimary">@color/MainBackground</item>
        <item name="android:textColorPrimary">@color/MainBackground</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

</resources>
